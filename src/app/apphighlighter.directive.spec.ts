import { ApphighlighterDirective } from './apphighlighter.directive';

describe('ApphighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ApphighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
